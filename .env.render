# Render.com Environment Variables Template
# Copy these to your Render service environment variables

# Database Configuration
USE_POSTGRES=true
APPLY_MIGRATIONS=true

# PostgreSQL Connection (Use Render's Internal Database URL)
# Format: Host=<internal-host>;Port=5432;Database=<dbname>;Username=<user>;Password=<password>;SSL Mode=Require
ConnectionStrings__PostgreSQL=Host=<YOUR_RENDER_POSTGRES_HOST>;Port=5432;Database=<YOUR_DB_NAME>;Username=<YOUR_DB_USER>;Password=<YOUR_DB_PASSWORD>;SSL Mode=Require

# JWT Configuration (CHANGE THESE IN PRODUCTION!)
JwtSettings__SecretKey=YourSuperSecretKeyThatIsAtLeast32CharactersLong!ChangeThisInProduction
JwtSettings__Issuer=FrozenApi
JwtSettings__Audience=FrozenApi
JwtSettings__ExpiryInHours=8

# CORS Configuration (Add your frontend URL)
ALLOWED_ORIGINS=https://your-frontend-app.onrender.com,http://localhost:3000

# ASP.NET Configuration
ASPNETCORE_ENVIRONMENT=Production

# Render automatically sets PORT environment variable
# The app will automatically bind to 0.0.0.0:$PORT
